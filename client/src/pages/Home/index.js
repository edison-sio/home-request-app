// Home page of the app.

const HomePage = (props) => {
    // Declare state variables for home page data.
    return (
        <div>
            Home Page
        </div>
    )
}

export default HomePage;